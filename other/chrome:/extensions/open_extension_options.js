javascript:var isCorrect,split1=document.location.href.split("?id=")[1];if(split1){if(confirm(`Is ${split1} Correct?`))chrome.developerPrivate.showOptions(split1,console.log(`LOADED EXTENSION VPAGE: ${split1}`));else{var split1=prompt("What is the ID of the extension");chrome.developerPrivate.showOptions(split1,console.log(`LOADED EXTENSION VPAGE: ${split1}`))}}else{var split1=prompt("What is the ID of the extension");chrome.developerPrivate.showOptions(split1,console.log(`LOADED EXTENSION VPAGE: ${split1}`))}
